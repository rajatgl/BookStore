package com.bridgelabz.bookstore.routes

class CartRoutes {

  /**
   *
   * TODO
   * All routes will need token => userId
   *
   * GET /cart => Seq[Cart]
   * GET /cart?cartId={id} => Option[Cart]
   * PUT /cart => insert a new cart or update existing cart
   * GET /cart/bill => {total: 100, tax: 20, delivery: 40, grandTotal: 160}
   * DELETE /cart => ability to delete a cart {input: cartId}
   * DELETE /cart/product => ability to delete a product from a cart {input: cartId and productId}
   */

}
